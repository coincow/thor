
class HoldingsResult:
    def __init__(self, code, holding, num, price, reason, exit):
        self.code = code
        self.holding = holding
        self.num = num
        self.price = price
        self.reason = reason
        self.exit = exit

