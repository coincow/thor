
class HoldingsResult:
    def __init__(self, code, holding, num, price, money, reason, exit):
        self.code = code
        self.holding = holding
        self.num = num
        self.price = price
        self.money = money
        self.reason = reason
        self.exit = exit

